<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- MusicSDK fetches search results and playable links over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Used by the settings System panel to report online/WiFi/Ethernet state. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required by Android's speech recognizer for voice search. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- audio_service runs playback in a foreground service so Android cannot
         kill the process while music is playing in the background. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Cần để app tự cài bản cập nhật tải từ GitHub Releases. Android 8+
         còn đòi người dùng cấp riêng quyền "cài ứng dụng không rõ nguồn gốc";
         canInstallPackages()/openInstallPermissionSettings() lo phần đó. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <application
        android:label="youcar"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- Playable links come from MusicSDK's extraction of third-party
             sources, so the scheme/host isn't known ahead of time; some
             resolve to plain http. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <!-- Routes steering-wheel and Bluetooth media keys into the app. -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
        <!-- Nhận kết quả phiên cài đặt của PackageInstaller. Với ứng dụng
             không có quyền hệ thống, commit() không tự cài: nó gửi trạng thái
             STATUS_PENDING_USER_ACTION kèm Intent hộp thoại xác nhận, và
             receiver này phải mở Intent đó lên. exported="false" vì chỉ hệ
             thống gửi lại PendingIntent do chính ứng dụng tạo ra. -->
        <receiver
            android:name=".InstallResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.viet_ktv.INSTALL_STATUS" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Impeller's Vulkan/GL backend renders visible artifacts (white
             fringing around blurred/rounded shadows, jagged glyphs) on
             emulators with virtualized GPUs such as BlueStacks. Fall back to
             the Skia renderer, which does not exhibit these artifacts. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
